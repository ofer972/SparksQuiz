DATABASE_URL=postgresql://postgres:password@localhost:5432/Quiz

# Auth
JWT_SECRET=change-me-to-a-long-random-string
DEFAULT_HOST_EMAIL=as@sparksquiz.local
APP_URL=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Email (Resend) — leave blank to print magic links to the backend console
RESEND_API_KEY=
RESEND_FROM=SparksQuiz <onboarding@resend.dev>

# Short join link (TinyURL) — leave blank to show long URL only
TINYURL_API_TOKEN=
